/**
 * @deprecated This file is superseded by React component:
 * - src/ui/BookmarkCleanupButtonPortal.jsx
 * - src/ui/BookmarkCleanupButton.jsx
 *
 * All bookmark cleanup functionality (extension check, button rendering,
 * event handling) is now handled by React.
 *
 * This file is kept only for backward compatibility with legacy imports.
 */

/**
 * @deprecated Use BookmarkCleanupButtonPortal React component instead.
 * This function is a no-op kept for backward compatibility.
 */
export function initBookmarkCleanup() {
  // No-op: React component handles all initialization
}
